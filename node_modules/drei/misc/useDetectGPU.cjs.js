'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var detectGpu = require('detect-gpu');

function useDetectGPU(props) {
  var GPUTier = React.useMemo(function () {
    var GPUTier = detectGpu.getGPUTier(props);
    return {
      isDesktop: GPUTier.tier.indexOf('DESKTOP') > -1,
      isMobile: GPUTier.tier.indexOf('MOBILE') > -1,
      tier: GPUTier.tier.split('_').pop(),
      type: GPUTier.type
    };
  }, [props]);
  return GPUTier;
}

exports.useDetectGPU = useDetectGPU;
